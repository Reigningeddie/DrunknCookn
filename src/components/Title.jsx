import neon from './title.module.css'

export default function Title() {

    return (
        <body>
        <container className={neon.title}>
            <h1 className={neon.neonTitle}>Drunkn'Cookn</h1>
        </container>
        </body>
    )
}